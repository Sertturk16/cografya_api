dist
node_modules
coverage
pnpm-lock.yaml
# Generated artifact — the openapi:generate script is its sole authority.
# Prettier must not reformat it, or it would drift from the generator output
# and trip the CI drift gate.
openapi

# Committed byte copy of a measured source, pinned by SHA-256 in
# `book-timestamps.artifact.ts`. `pnpm format` rewriting it would break the byte-copy
# invariant and make the seed refuse with "never hand-edited" — pointing the operator at
# the wrong cause. Same argument as `openapi` above, for a hash pin instead of a drift gate.
data/books/*.json
